import 'package:flutter/material.dart';

abstract final class AppColors {
  // Primary: warm purple (brand)
  static const primary = Color(0xFF6C5CE7);
  static const primaryLight = Color(0xFFA29BFE);
  static const primaryDark = Color(0xFF5A4BD1);

  // Backgrounds
  static const bgMain = Color(0xFFF8F9FE);
  static const bgCard = Color(0xFFFFFFFF);
  static const bgSurface = Color(0xFFF0EEFF);
  static const bgHover = Color(0xFFE8E5FF);

  // Semantic
  static const success = Color(0xFF00C853);
  static const successLight = Color(0xFFE8F5E9);
  static const successDark = Color(0xFF15803D);
  static const warning = Color(0xFFFFB300);
  static const warningLight = Color(0xFFFFF8E1);
  static const warningDark = Color(0xFFB45309);
  static const warningAccent = Color(0xFFD97706);
  static const danger = Color(0xFFFF5252);
  static const dangerLight = Color(0xFFFFEBEE);
  static const info = Color(0xFF2196F3);
  static const infoLight = Color(0xFFE3F2FD);

  // Text
  static const textPrimary = Color(0xFF2D2D3F);
  static const textSecondary = Color(0xFF6B7280);
  static const textTertiary = Color(0xFF7C8DB0);
  static const textMuted = Color(0xFF9CA3AF);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // Borders
  static const border = Color(0xFFE5E7EB);
  static const borderLight = Color(0xFFF3F4F6);
  static const borderPurple = Color(0xFFE0DBFF);

  // LINE OA
  static const lineGreen = Color(0xFF06C755);
  static const lineBg = Color(0xFFF7F8FA);
  static const chatBg = Color(0xFF7494A5);

  // Glassmorphism
  static const glassWhite = Color(0xB3FFFFFF);
  static const glassCardWhite = Color(0xD9FFFFFF);
  static const glassBorder = Color(0x99FFFFFF);

  // Gradient
  static const primaryGradientStart = Color(0xFF6C5CE7);
  static const primaryGradientEnd = Color(0xFFA29BFE);

  // Body background
  static const bgBody = Color(0xFFF4F7FE);
}
